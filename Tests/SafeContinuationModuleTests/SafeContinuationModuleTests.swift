import XCTest
@testable import SafeContinuationModule

final class SafeContinuationModuleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SafeContinuationModule().text, "Hello, World!")
    }
}
